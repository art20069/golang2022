go build
go build ldflage "-s -w"
using env: export GIN_MODE=release
using code: gin.SetMode(gin.ReleaseMode)



Deployment on Heroku
1. Pipeline set Create a github
2. Create Procfile


Heroku
$ heroku login
$ cd my-project/
$ git init
$ heroku git:remote -a golang2022
Deploy your application
$ git add .
$ git commit -am "make it better"
$ git push heroku master

ต้องแก้ go verstion ให้ตรงกับ go.Mod
